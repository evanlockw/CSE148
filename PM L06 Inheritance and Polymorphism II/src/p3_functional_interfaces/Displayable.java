package p3_functional_interfaces;

public interface Displayable {
	// if an interface only has one abstract method, it is called functional interface.
	void display();
	// A functional interface can have as many static and/or default methods as needed.

}
